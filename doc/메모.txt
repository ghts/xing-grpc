- 프론트엔드 스레드
  : gRPC 요청 수신 후 백엔드 스레드로 전달.
  : 백엔드 스레드에서 응답값을 받은 후 gRPC로 전달.

- 백엔드 스레드
  : 질의 수신 채널 모니터링.
  : API 호출
  : 윈도우 메시지 처리 (이 과정에서 콜백 함수도 실행).
  : 콜백 함수로 수신한 결과를 조합하여 응답값을 생성하여 채널을 통해서 프론트엔드 스레드로 전송.
